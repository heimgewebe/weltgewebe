---
name: PR1596 Product Log Diagnostic
# activation revision: 2

permissions:
  actions: read
  contents: read

"on":
  pull_request:
    branches: [main]
    paths:
      - '.github/workflows/pr1596-product-log-diagnostic.yml'

jobs:
  read-tail:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Read product convergence failure tail
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          gh api \
            -H 'Accept: application/vnd.github+json' \
            /repos/${GITHUB_REPOSITORY}/actions/jobs/90210728355/logs \
            > /tmp/pr1596-product.log
          echo 'BEGIN_PR1596_PRODUCT_TAIL'
          tail -n 100 /tmp/pr1596-product.log
          echo 'END_PR1596_PRODUCT_TAIL'
