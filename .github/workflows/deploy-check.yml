---
name: Deploy Check
permissions:
  contents: read

"on":
  push:
    branches:
      - main
  pull_request:

jobs:
  csp-contract-check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: CSP contract check
        run: scripts/preflight/csp_contract_static.sh
