{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "description": "Unified DocMeta schema — single canonical model for all repo documents.",
  "type": "object",
  "properties": {
    "id": {
      "type": "string",
      "minLength": 1
    },
    "title": {
      "type": "string",
      "minLength": 1
    },
    "summary": {
      "type": "string",
      "minLength": 1
    },
    "status": {
      "type": "string",
      "enum": [
        "canonical",
        "active",
        "deprecated",
        "draft"
      ]
    },
    "doc_type": {
      "type": "string",
      "minLength": 1
    },
    "relates_to": {
      "type": "array",
      "items": {
        "type": "string",
        "minLength": 1
      },
      "description": "Related document paths (repo-root-relative) for cross-linking and backlink generation."
    },
    "supersedes": {
      "type": "array",
      "items": {
        "type": "string",
        "minLength": 1
      }
    },
    "depends_on": {
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "role": {
      "type": "string",
      "enum": [
        "norm",
        "reality",
        "runbooks",
        "action"
      ],
      "description": "Zone-specific: document role within the zone hierarchy."
    },
    "organ": {
      "type": "string",
      "minLength": 1,
      "pattern": "^[a-z0-9-]+$",
      "description": "Zone-specific: organizational unit."
    },
    "last_reviewed": {
      "type": "string",
      "pattern": "^\\d{4}-\\d{2}-\\d{2}$"
    },
    "verifies_with": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "Zone-specific: scripts that verify this document."
    },
    "audit_gaps": {
      "type": "array",
      "items": {
        "type": "string"
      }
    }
  },
  "required": [
    "id",
    "title",
    "status",
    "summary"
  ],
  "additionalProperties": false
}
