{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://weltgewebe.org/schemas/domain/role.schema.json",
  "title": "Role",
  "description": "A role controlling permissions for a user or system actor in the Weltgewebe.",
  "type": "object",
  "properties": {
    "id": {
      "type": "string",
      "format": "uuid"
    },
    "user_id": {
      "type": "string",
      "format": "uuid"
    },
    "permissions": {
      "type": "object",
      "description": "Intentional extension surface: permission flags and scopes for this role. Keys stay deliberately open so the permission model can evolve without a schema change; values are constrained to booleans (flags), strings (single scope) or string arrays (scope lists) so that no arbitrary nested structures can be stored.",
      "additionalProperties": {
        "type": [
          "boolean",
          "string",
          "array"
        ],
        "maxLength": 128,
        "items": {
          "type": "string",
          "minLength": 1,
          "maxLength": 128
        },
        "maxItems": 50
      }
    },
    "created_at": {
      "type": "string",
      "format": "date-time"
    }
  },
  "required": [
    "id",
    "user_id",
    "permissions",
    "created_at"
  ],
  "additionalProperties": false
}
