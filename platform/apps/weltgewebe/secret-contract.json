{
  "schema_version": 1,
  "secret_name": "weltgewebe-runtime",
  "namespace_scope": "overlay-owned",
  "required_keys": [
    "database-url"
  ],
  "optional_keys": [
    "smtp-host",
    "smtp-port",
    "smtp-user",
    "smtp-password",
    "smtp-from",
    "federation-signing-key-b64"
  ],
  "rules": [
    "No Secret object or concrete secret value is stored in Git.",
    "Local and CI values are generated ephemerally by the reference bootstrap.",
    "Staging and production require an external secret controller or an equivalent audited injection path.",
    "Missing required keys must stop rollout readiness.",
    "Federation signing material must be provided only when the complete cell identity and explicit proxy trust decision are configured."
  ]
}
