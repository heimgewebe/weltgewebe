---
apiVersion: v1
kind: Service
metadata:
  name: weltgewebe-web
  labels:
    app.kubernetes.io/name: weltgewebe-web
spec:
  selector:
    app.kubernetes.io/name: weltgewebe-web
  ports:
    - name: http
      port: 8080
      targetPort: http
      protocol: TCP
