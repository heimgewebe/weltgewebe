# Weltgewebe repository ignores
# Prevent accidental check-in of local Git directories
/.git/

# Environment files
.env
.env.local
.env.*
!.env.example

# Node/NPM artifacts
node_modules/
npm-debug.log*

# Build outputs
build/
dist/
.tmp/
target/

# OS files
.DS_Store
Thumbs.db
metrics.json

# Python bytecode
__pycache__/
*.pyc
*.pyo

# --- Demo / local runtime artifacts (do NOT commit) ---
# We keep the folder structure, but demo inputs are generated at runtime.
.gewebe/in/
apps/web/.gewebe/in/

# Local dev logs (generated by running demo/dev servers)
*.log

# If you ever want to commit a specific log for docs/debug, negate it explicitly:
# !docs/logs/example.log
