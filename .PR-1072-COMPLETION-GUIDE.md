# How to Complete PR #1072 Update

This file guides you through the final step: updating the PR description on GitHub with the dependency footprint analysis.

## Current Status

✅ All code is implemented and validated  
✅ All tests pass (146 unit, 6 integration tests self-migrating)  
✅ All gates green (fmt, clippy, test, diff-check)  
✅ Dependency footprint proven via `cargo tree -i` (all new transitive deps are metadata-only)  
✅ Branch pushed with documentation commits (2d5baa8f)  

⏳ **Pending:** Update PR description on GitHub with dependency analysis section

## Option 1: Using the Automated Script (Requires GH_TOKEN)

If you have a GitHub personal access token:

```bash
export GH_TOKEN="your-github-token-here"
./update_pr_1072_body.sh
```

The script will:
1. Read the updated PR body from the embedded content
2. Use GitHub REST API to update PR #1072
3. Confirm success

**To get a GH_TOKEN:**
1. Go to https://github.com/settings/tokens/new
2. Select scope: `repo` (for public repos)
3. Generate and copy the token
4. Run the command above

## Option 2: Manual Update via GitHub UI (No Token Needed)

This is the simplest approach if you don't have a token:

### Step 1: Copy the Updated PR Body

The updated PR description (with Dependency Footprint Analysis section) is ready in this repo:

```bash
cat .PR-1072-UPDATED-BODY.txt
```

Or view it directly: [.PR-1072-UPDATED-BODY.txt](.PR-1072-UPDATED-BODY.txt)

### Step 2: Go to the PR on GitHub

Open: https://github.com/heimgewebe/weltgewebe/pull/1072

### Step 3: Edit the Description

1. Click the **three dots (...)** in the top-right of the PR description
2. Click **"Edit"**
3. Select all the text (Ctrl+A / Cmd+A)
4. Delete it
5. Paste the content from `.PR-1072-UPDATED-BODY.txt`
6. Click **"Save"**

## Option 3: Using GitHub CLI

If you have `gh` installed and authenticated:

```bash
gh auth login  # if not already authenticated
gh pr edit 1072 --body-file .PR-1072-UPDATED-BODY.txt
```

## After PR Update: Next Steps

Once the PR description is updated on GitHub:

1. **Mark PR as "ready for review"**
   - On the PR page, click "Ready for review" button (or toggle draft status)
   - This triggers CI pipeline

2. **Await CI Results**
   - All gates should pass (same as local validation)

3. **Human Review & Approval**
   - Reviewers will see the updated description with dependency analysis
   - Upon approval, merge the PR

## What Changed in the Updated Description

The updated body adds a new section: **"Dependency Footprint Analysis"**

This section documents:
- The transitive dependencies added by `sqlx/migrate`
- The `cargo tree -i` proofs showing no active linkage
- Acceptance rationale (unavoidable cost of startup migrations)

This addresses the earlier concern about the Cargo.lock expansion and proves it's acceptable.

## Troubleshooting

**Q: I can't find the "Edit" button**  
A: You must be the PR author or have repo write permissions. Contact repo maintainers if needed.

**Q: The GH_TOKEN script didn't work**  
A: Verify the token has `repo` scope and is correctly exported:
```bash
echo $GH_TOKEN  # should print your token, not empty
```

**Q: I'm missing files (.PR-1072-UPDATED-BODY.txt or update_pr_1072_body.sh)**  
A: Ensure you've pulled the latest commits from the branch:
```bash
git pull origin feature/auth-phase5-db-session-store
```

---

**This is the final step before merge. All code and validation work is complete.**

For questions, refer to:
- `.pr-1072-final-status.md` — Comprehensive merge-readiness report
- `/memories/session/pr-1072-dependency-proof.md` — Detailed findings from validation
