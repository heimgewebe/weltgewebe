# Sprites

Dieses Verzeichnis dient der lokalen Ablage von MapLibre-kompatiblen Spritesheets (`sprite.png`, `sprite.json`, `sprite@2x.png`, `sprite@2x.json`).
Die MapLibre `style.json` ist auf den relativen Pfad `./sprites/sprite` konfiguriert.
Die konkreten Assets fehlen noch (siehe `map-style/ASSETS.md`).
