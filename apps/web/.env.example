# API Base URL for the frontend to connect to.
# For local development with 'just up', this should be http://localhost:8080 (via proxy) or http://localhost:8080 (direct).
# In production, this is usually empty or relative to domain.
PUBLIC_GEWEBE_API_BASE=http://127.0.0.1:8080
