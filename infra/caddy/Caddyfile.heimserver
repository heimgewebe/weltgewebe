# Reference only, not deployed from this repo. Integration logic resides in the external Heimserver repository.

# Reference for Weltgewebe API (internal access)
api.weltgewebe.home.arpa {
	# Upstream verified in weltgewebe repo (compose.prod.yml): service=api port=8080
	reverse_proxy api:8080
	tls internal
}
